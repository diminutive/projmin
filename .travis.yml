language: r
warnings_are_errors: true
sudo: true
dist: bionic  # 16.04

before_install:
  - sudo add-apt-repository ppa:ubuntugis/ubuntugis-unstable --yes
  - sudo apt install --assume-yes libproj-dev
  

after_success:
  - Rscript -e 'covr::codecov()'

